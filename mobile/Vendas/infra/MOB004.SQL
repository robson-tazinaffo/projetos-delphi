/* TABELA DE PRODUTOS */
CREATE TABLE MOB004 
(
     PRO_ID INTEGER UNIQUE NOT NULL, 
     PRO_EMPRESA INTEGER NOT NULL,  
     PRO_CODIGO VARCHAR(30) NOT NULL,
     PRO_DESCRICAO VARCHAR(100) NOT NULL,
     PRO_UNIDADEMEDIDA VARCHAR(6),
     PRO_ATIVOINATIVO CHAR(1) DEFAULT 'A' NOT NULL, 
     PRO_PRCVENDA NUMERIC(18,4) NOT NULL DEFAULT 0,
     PRO_DESC_MAXIMO NUMERIC(4,2) DEFAULT 0,
     PRO_SALDO_DISPONIVEL NUMERIC(18,2) DEFAULT 0 NOT NULL
);
