/* TABELA DE PEDIDOS */
CREATE TABLE MOB009 
( 
	PED_ID INT UNIQUE NOT NULL,
	PED_DTPEDIDO TIMESTAMP NOT NULL,
	PED_ID_PESSOA INT NOT NULL,
	PED_ID_CONTATO INT,
	PED_ID_COND_PAGTO INT,
	PED_STATUS CHAR(1) DEFAULT 'L',
	PED_VENDEDOR INT,
	PED_DTALTERACAO TIMESTAMP,
	PED_VLRTOTALBRUTO  NUMERIC(18,2) DEFAULT 0,
	PED_VLRTOTALLIQUIDO NUMERIC(18,2) DEFAULT 0,
	PED_VLRIPI NUMERIC(18,2) DEFAULT 0,
	PED_VLRTOTALICMSST NUMERIC(18,2) DEFAULT 0,
	PED_SITUACAO VARCHAR(10),
    FOREIGN KEY (PED_ID_PESSOA) REFERENCES MOB001 (PES_ID) 
);

