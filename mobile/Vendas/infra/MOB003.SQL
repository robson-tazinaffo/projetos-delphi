/* TABELA DE SITUAÇÃO FINANCEIRA */
CREATE TABLE MOB003 ( 
   SIF_ID integer UNIQUE NOT NULL, 
   SIF_ID_PESSOA integer NOT NULL, 
   SIF_TOTAL_ABERTO numeric(18,2), 
   SIF_TOTAL_ATRASADO numeric(18,2), 
   SIF_LIMITE_CREDITO numeric(18,2), 
   SIF_SALDO_LIMITE_CREDITO numeric(18,2), 
   FOREIGN KEY (SIF_ID_PESSOA) REFERENCES MOB001 (PES_ID) 
);
